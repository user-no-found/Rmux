#!/bin/bash

### This script is called after the application is upgraded.
### 更新后处理：清理标记文件，重建必要的目录

# 加载公共函数库
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
source "${SCRIPT_DIR}/common"

# 调用公共函数
upgrade_callback
