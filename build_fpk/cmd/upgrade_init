#!/bin/bash

### This script is called before the application is upgraded.
### 更新前准备：创建更新标记，防止卸载时删除数据

# 加载公共函数库
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
source "${SCRIPT_DIR}/common"

# 调用公共函数
upgrade_init
